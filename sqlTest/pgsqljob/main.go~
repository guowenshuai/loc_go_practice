package main

import (
	"fmt"
	"database/sql"
	_ "github.com/lib/pq"
)

func main() {
	db, err := sql.Open("postgres", "user=gws password=113414 dbname=test sslmode=disable")
	checkErr(err)

	stmt
}


	


func checkErr(err error) {
	if err != nil {
		panic(err)
	}
}
